name: Generate and update external help, v3

on:
  push:
    paths:
    - 'docs/v3/**'
    - 'source'
    branches:
    - 'release3'

jobs:
  buildHelp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{ secrets.GITAUTOCOMMITPAT }}

      - name: Build temp module
        run: ./build/tempbuild_github.ps1
        shell: pwsh

      - name: Generate and update docs
        run: ./build/generateAndUpdateExternalHelpv3.ps1
        shell: pwsh

      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Generate and update help
          commit_user_name: My GitHub Actions Bot
          file_pattern: docs
          push_options: --force
